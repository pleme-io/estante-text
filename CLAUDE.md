# estante-text — Claude Orientation

Four batteries (`json` / `yaml` / `csv` / `text`). Each gracefully
degrades to coreutils when the modern tool isn't installed. Typed
SDLC manifest at `pleme-caixa.lisp`.
